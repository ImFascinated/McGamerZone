package zone.themcgamer.core.discord;

import org.bukkit.plugin.java.JavaPlugin;
import zone.themcgamer.core.module.Module;
import zone.themcgamer.core.module.ModuleInfo;

@ModuleInfo(name = "Discord Link Manager")
public class DiscordLinkManager extends Module {
    public DiscordLinkManager(JavaPlugin plugin) {
        super(plugin);
    }
}